# Backend (Maven / Spring Boot)
primatis-api/target/

# Frontend (Angular / Node)
primatis-web/dist/
primatis-web/node_modules/
primatis-web/.angular/

# Data seeding (Python)
data-seeding/.venv/
data-seeding/__pycache__/
data-seeding/**/__pycache__/
data-seeding/data/raw/
data-seeding/data/normalized/
data-seeding/data/validated/
data-seeding/output/
data-seeding/logs/

# Local secrets / environment
.env
.env.local
*.local.properties
application-local.yml
application-local.properties

# Clés RSA JWT (DEV-03.7) : aucune clé privée, réelle ou de test, n'est
# versionnée (contexte maître §7.27). Les tests génèrent leur paire RSA en
# mémoire (JwtTestKeysConfig) ; aucune exception à cette règle.
*.pem
*.key

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Local PostgreSQL backups
db_backup/
