# PRIMATIS — ASSET-RESET-02.2 — Catalogue

Pack spécifique à la page Catalogue, produit depuis `docs/design/public/catalogue.png` comme référence visuelle. Il complète ASSET-RESET-01 et ne modifie pas Angular.

## Contenu

- `source/` : master PNG de la nouvelle scène bibliothèque/sphère armillaire ;
- `hero/` : variantes WebP desktop, tablette et mobile ;
- `docs/` : manifeste, provenance, prompt et contrat d'intégration ;
- `preview/` : planche de validation visuelle.

## Ce qui n'est pas dupliqué

- couvertures : données réelles via `coverImageUrl` ;
- couverture absente et état vide Catalogue : ASSET-RESET-01 ;
- recherche, filtres, tri et navigation : SVG globaux ou PrimeIcons ;
- footer : assets globaux/Home validés selon le futur PublicShell.

## Interdictions

- aucune pastille de disponibilité sans donnée backend ;
- aucun favori/bookmark non fonctionnel ;
- aucune catégorie ou quantité inventée ;
- aucun texte incorporé dans le hero.

```text
ASSET-RESET-02.2 — PASS FINAL — APPROVED
```
